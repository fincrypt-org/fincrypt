// Login is a placeholder until Phase 2 wires OPAQUE auth.
export default function Login() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Log in</h1>
      <p>Authentication arrives in Phase 2 (OPAQUE — the password never leaves the device).</p>
    </main>
  )
}
